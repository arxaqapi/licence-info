#include "2opt.h"

bool does_it_cross(int n_1, int n_2, instance_t instance)
{
    return false;
}


void two_opt(int *nodes, instance_t instance)
{
    bool upgrade = true;

    while (upgrade)
    {
        for (int i = 0; i < instance.dimension; i++)
        {
            for (int j = i + 1; j < instance.dimension; j++)
            {
                
            }
            
        }
        
    }
    
}