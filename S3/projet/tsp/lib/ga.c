#include "ga.h"



void dpx()
{
    
}


double ga()
{
    double current_distance;

    int n_individus = 20;
    int n_croisement; // 1 to pop/2
    float taux_mutation = 0.3;
    int n_generations = 200;

    // create initial population N indiv randomly initialized

    /*
    repeat
    while (nb croisement voulu non atteint)
        selec ransomly 2 indiv
        faire croisement qui donne tournée fille
        avec une prob p, faire muter la tournée fille
        remplacer une indiv de la pop par la fille (au hasard ou le moins performant)
    end
    until: fixed nb de géné OU stabilité atteinte
    */

    return current_distance;
}